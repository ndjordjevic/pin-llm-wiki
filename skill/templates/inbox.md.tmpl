# Inbox

Drop URLs below under `## Pending`. Run `/pin-llm-wiki add <url>` for single-URL ingest, or edit this file and run `/pin-llm-wiki run` to batch-process all pending items.

## Pending

<!-- Add URLs here, one per line, as markdown checkboxes.
     Supported inline tags:
       <!-- detail:brief -->        override detail level for this source
       <!-- detail:standard -->
       <!-- detail:deep -->
       <!-- branch:dev -->          GitHub: use this branch instead of default
       <!-- clone -->               GitHub deep: full git clone to raw/github/<org>-<repo>/
       <!-- skip -->                skip this URL on the next run
-->

## Completed

<!-- Processed lines are moved here automatically. Format after ingest:
       - [x] https://... <!-- ingested YYYY-MM-DD -->
     To re-fetch an already-ingested source, add <!-- refresh --> to its line:
       - [x] https://... <!-- ingested YYYY-MM-DD --><!-- refresh -->
     Then run `/pin-llm-wiki run`. The tag is removed automatically after re-fetch.
-->
